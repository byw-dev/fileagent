# 设计差异全面分析 — 范围与计划

> **日期**：2026-07-03
> **背景**：项目进入 Phase 3 后期，表面上任务逐项完成，但实际使用中"不是缺功能就是有 Bug，
> 要么设计粗糙未覆盖实际需求"，推进陷入僵局。本系列报告逐模块对照
> `docs/design/system-design.md` 与实际实现，厘清差异并给出破局建议。
> **方法**：逐步输出、增量保存，便于跨会话接续。

---

## 初步侦察结论（2026-07-03，粗查）

### 表面健康的部分

- `controlplane` 与 `agent` 均编译通过（`go build ./...` exit 0）。
- `docs/tasks/bugs/open.md` 当前无未解决 Bug。
- 早期审计报告 `phase3-readiness-audit.md` 指出的功能缺口经抽查**均已修复**：
  - B-1：预签名 URL 现使用 `bucket.Name`（`files.go:258`），不再是 UUID。
  - B-2：presign TTL 已改为 15 分钟（`files.go:257`）。
  - B-5：`tail` / `close_wait` 已在 watcher/uploader/queue 中实现。
  - B-6：退避表已改为设计要求的 30s→2min→10min→30min→2h（`engine.go:175`）。

### 支持"僵局"判断的信号

1. **每个任务都衍生 FIX/BUG 系列**（修复循环模式）：
   - T3-1 → T3-1-FIX → T3-1-BUGFIX
   - T3-2 → T3-2-FIX（A~L 共 12 项 API 契约错位）→ T3-2-BUG（A~D，4 项 P0/P1）
   - T3-5 → T3-5-BUG 系列 → 专项审计 → T3-5-IMPL-J 重设计
   - T3-6 → T3-6-BUG-A/B
   - 模式：实现"完成"（单测绿）→ 人工联调 → 爆出一串 P0。说明**"完成"的定义
     只覆盖到单元测试，缺少机器可执行的端到端验收**。
2. **根因已被自己诊断出来，却压成 P3 技术债**（`backlog.md` 末尾）：
   OpenAPI 契约自动校验、MSW 真实 API 格式集成测试——正是 T3-2-FIX 那 12 项
   契约错位的病根，却排期到 Phase 4。
3. **任务文档三处状态互相矛盾**：
   - `CLAUDE.md`："T3-2 进行中"
   - `phase-3.md`："当前优先 T3-6 ⬜"
   - `active.md`："T3-6 ✅，当前任务 T3-3"
   AI Agent 的输入文件本身就是过时的，文档维护成本已跟不上迭代速度。
4. **本地环境状态与仓库脱节**（`git status` 未跟踪文件）：
   `agent/config.toml`、`deploy/config/controlplane.env`、
   `controlplane/bootstrap_admin_credentials.txt`（⚠️ 凭据文件躺在仓库工作区）。
   联调环境不可复现，可能是"每次联调都爆新问题"的诱因之一。
5. **设计↔实现双向漂移**：D-009 字段统一（T3-5）本身就是对漂移的补救；
   设计文档 2327 行，哪些章节已过时、哪些实际需求设计未覆盖，需逐章对照。

### 规模基线

| 模块 | 规模 | 状态 |
|------|------|------|
| controlplane + agent + pkg | 13.4k LOC（另 14.8k 测试） | 编译通过 |
| webui | 6.3k LOC (ts/tsx) | — |
| sdk/python | 2.9k LOC | T3-3 联调未开始 |
| sdk/java | 未开工 | 计划内（T4-4） |

---

## 报告结构（增量输出）

| 文件 | 内容 | 状态 |
|------|------|------|
| `00-scope-and-plan.md` | 本文件：范围、初步侦察、计划 | ✅ |
| `01-controlplane.md` | 设计 §5/§6 vs `controlplane/` 逐条对照 | ✅ |
| `02-agent.md` | 设计 §4 vs `agent/` 逐条对照 | ✅ |
| `03-webui.md` | 设计 §7 vs `webui/` 逐条对照 | ✅ |
| `04-sdk-python.md` | 设计 §8 vs `sdk/python/` 逐条对照 | ✅ |
| `05-contracts.md` | proto / REST / DB schema / 分页 跨模块契约一致性 | ✅ |
| `06-e2e-verification.md` | 动态验证：本地起服务实测的结果 | ✅ |
| `07-summary.md` | 总结论：差异清单汇总 + 僵局根因 + 破局建议 | ✅ |

**差异标记约定**：
- ✅ 与设计一致
- ⚠️ 部分实现 / 与设计有出入
- ❌ 设计有、实现缺失或错误
- 📝 实现有、设计未覆盖（设计粗糙/滞后）

---

## 用户已确认（2026-07-03）

1. **验证方式**：允许动态验证——可启动 `docker-compose.dev.yml`、构建二进制、
   真实走完主链路。
2. **僵局表现**：三类问题都存在——
   - 联调时功能反复坏（任务标记完成后实际操作又爆新 Bug）；
   - 设计不满足实际需求（按设计实现后与实际使用对不上，需返工）；
   - 文档与代码脱节（不确定项目真实进度，AI Agent 输入过时）。

   → 分析需同时回答三个问题：**实现与设计差在哪**、**设计与实际需求差在哪**、
   **文档与现实差在哪**。
