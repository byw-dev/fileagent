import { Typography } from 'antd'

/**
 * Pending agents page — lists agents awaiting approval (placeholder).
 */
function AgentsPendingPage() {
  return (
    <div>
      <Typography.Title level={3}>待审批采集器</Typography.Title>
      <Typography.Text type="secondary">页面开发中（Phase 2）</Typography.Text>
    </div>
  )
}

export default AgentsPendingPage
