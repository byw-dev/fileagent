import { Typography } from 'antd'

/**
 * Create event rule page (placeholder).
 */
function EventCreatePage() {
  return (
    <div>
      <Typography.Title level={3}>创建事件规则</Typography.Title>
      <Typography.Text type="secondary">页面开发中（Phase 2）</Typography.Text>
    </div>
  )
}

export default EventCreatePage
