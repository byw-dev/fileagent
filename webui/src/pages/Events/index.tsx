import { Typography } from 'antd'

/**
 * Event rules list page (placeholder).
 */
function EventsPage() {
  return (
    <div>
      <Typography.Title level={3}>事件规则</Typography.Title>
      <Typography.Text type="secondary">页面开发中（Phase 2）</Typography.Text>
    </div>
  )
}

export default EventsPage
